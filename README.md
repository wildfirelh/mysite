# My Personal Site

个人主页，使用原生 HTML、CSS 和 JavaScript 构建，无需安装依赖或执行构建命令。

## 本地预览

直接在浏览器中打开 `index.html` 即可。

## 目录结构

- `index.html`：页面结构与内容
- `assets/css/site.css`：样式与响应式布局
- `assets/js/site.js`：标签切换、技能详情、状态弹窗和壁纸逻辑

## 部署

推送至 `master` 分支后，GitHub Actions 会自动部署 GitHub Pages。
